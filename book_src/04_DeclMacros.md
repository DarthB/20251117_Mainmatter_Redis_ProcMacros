# Declarative Macros

> Procedural Macros aren't declarative Macros (Pattern Matching).

```rust
let v = vec![1, 4, 7];
println!("{:?}", v);
```

- They are not distungishable for a caller from Procedural Function-like Macros.